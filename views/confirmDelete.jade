include includes/menu
h1= title
#content
  form(action="/movie/delete", method="post")
    div Are you certain you want to delete <em>#{title}</em>? This action cannot be undone.
    input(name="movieId", type="hidden", value="#{_id}")
    input(name="prevPage", type="hidden", value="#{currentUri}")
    input(name="submit", type="submit", value="Delete")
    input(name="submit", type="submit", value="Cancel")