h1= title
include includes/menu
#content
  each tag in tags
    .tag-wrapper
      .tag-name
        a(href="/movie/tag/#{tag._id}")= tag.title
      .edit-tag
        a(href="#") Edit
      //-
        .remove-tag
          a(href="#") Remove